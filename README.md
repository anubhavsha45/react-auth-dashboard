# react-auth-dashboard
A full-stack authentication dashboard built with React and Node.js. Features JWT-based login/register, protected routes, user profile, and CRUD task management with search and filtering.
